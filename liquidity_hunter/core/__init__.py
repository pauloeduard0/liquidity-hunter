"""Core layer: framework-agnostic domain models and shared abstractions.

Nothing in this package may depend on `app`, `data`, `indicators`,
`liquidity`, `psychology`, or `scoring`. All other layers
may depend on `core`.
"""
